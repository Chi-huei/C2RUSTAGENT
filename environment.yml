name: rustagent

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - pip:
      - langchain>=0.3.0
      - langchain-anthropic>=0.3.0
      - langgraph>=0.2.0
      - python-dotenv>=1.0.0